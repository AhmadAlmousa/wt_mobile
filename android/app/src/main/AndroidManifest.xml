<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The whole app is an HTTP client for a webtrees site. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- The device now holds the tree (Phase 10c), so it must not leave it.

         `sync_eval.md` §6 #3 calls keeping a whole visible tree on a phone
         "a real change in exposure" — until Phase 10 the app held only what it
         had fetched, in RAM. The file is encrypted and its key lives in the
         Android keystore, which is already enough; these two attributes close
         the case that remains, which is a backup or an `adb backup` carrying
         the ciphertext off the device to be worked on at leisure.

         allowBackup="false" also spares a restored phone a file it can never
         open: the keystore key does not travel with a backup, so a restored
         store would be permanently unreadable ciphertext. Better that it is
         simply not there, and the next sync makes a new one. -->
    <application
        android:label="webtrees"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
